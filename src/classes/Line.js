export default class Line {

    constructor(color, height) {
        this.color = color
        this.height = height
    }

    setHeight = (height) => {
        this.height = height
    }

    setColor = (color) => {
        this.color = color
    }

}